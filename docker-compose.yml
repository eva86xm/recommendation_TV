services:
  signage:
    build: .
    container_name: digital-signage
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./instance:/app/instance
      - ./uploads:/app/uploads
